<nav class="navbar navbar-expand-md sticky-top bg-body-tertiary shadow-sm py-2">
    <div class="container-fluid px-4 max-21-9">

        <a href="/" class="navbar-brand fw-bold text-decoration-none d-flex align-items-center gap-2">
            <i class="bi bi-house-door-fill fs-4 text-secondary"></i>
            <span class="fs-5">{{ t("global.home") }}</span>
        </a>
        <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-center" id="mainNavbar">
            <ul class="navbar-nav align-items-center gap-3">
                <li class="nav-item"></li>
            </ul>
        </div>

        <div class="d-flex align-items-center gap-2 ms-auto">
            <button type="button" class="btn btn-outline-secondary rounded shadow-sm border-0" id="theme-toggle-btn" title="Changer de thème">
                <i class="bi bi-sun-fill"></i>
            </button>

            <button type="button" class="btn btn-outline-secondary rounded shadow-sm border-0" id="change-lang-btn">
                {{ t("global.change_language") }}
            </button>
        </div>
    </div>
</nav>
