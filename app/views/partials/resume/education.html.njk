<div class="col-sm-12 col-lg-6">
    <div class="card h-100 shadow-sm border-1 rounded-3">
        <div class="card-body d-flex flex-column">
            <h4 class="card-title mb-2">{{ education.degree }}</h4>
            <h6 class="card-subtitle mb-2 text-muted">
                {{ education.school }}
                —
                <span class="fst-italic">{{ education.period }}</span>
                <br>
                <small>{{ education.location }}</small>
            </h6>

            <ul>
                {% for task in education.description %}
                    <li>{{ task }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>
